# Copyright, License, and Asset Policy

상태: `Approved — 2026-09-06`

## 권리 정책

Copyright © 2026 yhames. All rights reserved.

이 저장소에는 오픈 소스 라이선스나 그 밖의 일반 사용 허가를 부여하지 않는다. 루트에 `LICENSE` 파일을 두지 않는 것은 의도된 정책이다. 공개 열람 가능 여부와 별개로, 별도의 서면 허가 없이 저장소의 코드, 문서, 디자인 또는 에셋을 복제, 수정, 배포, 재라이선스하거나 파생물을 만들 수 없다.

GitHub에서 저장소를 열람하거나 fork하는 행위에는 GitHub Terms of Service가 적용된다. 이는 저장소 소유자가 추가적인 저작권 이용 허가를 부여한다는 뜻이 아니다.

## 적용 범위

위 정책은 이 저장소가 자체 제작한 다음 콘텐츠에 적용된다.

- FastAPI 및 React 애플리케이션 소스
- 테스트, 자동화, GitHub Actions와 Codex Skill
- 제품 사양, Roadmap, UI 사양과 개발 문서
- Studentory용 CSS 디자인과 `frontend/public/favicon.svg`

외부 패키지와 도구는 이 정책의 적용 대상이 아니다. 각 저작권자와 패키지의 자체 라이선스가 적용된다.

## 에셋 감사

2026-09-06 현재 Git이 추적하는 이미지·아이콘·폰트·미디어 파일을 확장자와 소스 참조 기준으로 검사했다.

| 경로 | 종류 | 출처 및 권리 | 배포 판단 |
| --- | --- | --- | --- |
| `frontend/public/favicon.svg` | 단순 벡터 워드마크 | Studentory 저장소에서 직접 작성 | 저장소 자체 콘텐츠로 포함 |

다음 항목은 발견되지 않았다.

- 외부 사진, 캐릭터, 로고 또는 브랜드 SVG
- 외부 웹폰트나 로컬 폰트 파일
- Keepers Note의 이미지, 문구 또는 고유 브랜드 자산
- 배포용 PDF, 영상 또는 음원
- 출처가 불명확한 fixture나 sample 에셋

새 에셋을 추가할 때는 출처, 작성자, 사용 조건과 재배포 가능 범위를 이 문서에 기록한다. 조건을 확인할 수 없는 에셋은 추가하지 않는다.

## 의존성 감사

현재 직접 의존성의 설치된 패키지 메타데이터와 lockfile을 확인했다.

- Frontend 직접 의존성 및 개발 도구: MIT 또는 Apache-2.0
- Backend 직접 의존성 및 개발 도구: MIT, BSD 또는 BSD-3-Clause
- `loguru` 패키지 메타데이터 분류: MIT

`frontend/node_modules`와 `backend/.venv`는 Git에서 제외되며 이 저장소에 제3자 패키지 소스를 복사해 배포하지 않는다. `frontend/pnpm-lock.yaml`과 `backend/uv.lock`은 버전 재현을 위한 메타데이터다. 저장소의 권리 정책은 외부 의존성의 라이선스를 변경하거나 제한하지 않는다.

애플리케이션 빌드 결과, 설치 패키지, 컨테이너 또는 실행 서비스를 별도로 배포할 때는 해당 시점의 전체 전이 의존성, notice 제공 의무와 배포 형태를 다시 감사한다.

## 변경 절차

다음 변경은 별도의 명시적 결정과 권리 감사를 요구한다.

- `LICENSE`, `NOTICE` 또는 오픈 소스 라이선스 추가
- 외부 이미지, 아이콘, 폰트, 음원, 영상 또는 문서 추가
- 제3자 코드를 저장소에 직접 복사하거나 수정해 포함
- 애플리케이션 빌드 결과나 설치 패키지의 공개 배포
- 저작권자 또는 사용 허가 범위 변경

