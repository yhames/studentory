# 수업 현황 주간 시간표 고도화 구현 명세

## 1. 문서 목적

이 문서는 현재 `codex/uir-001-ui-refactor` 작업 트리를 기준으로 수업 현황을 주간 시간표 형태로 고도화하기 위한 구현 명세다.

Codex는 이 문서를 구현 요청으로 사용할 때 먼저 저장소와 미커밋 변경을 분석하고, 기존 `UIR-001` 작업을 보존한 상태에서 가장 작은 교차 스택 변경을 수행한다.

첨부 이미지는 다음 정보 구조만 참고한다.

- 요일을 가로 열로 배치하는 주간 보기
- 요일별로 구분되는 부드러운 배경과 카드
- 학생 식별 정보와 짧은 배지를 한눈에 읽는 구조

이미지의 브랜드 자산, 고유 문구, 아이콘, 색상값, 장식과 화면 구성을 그대로 복제하지 않는다. Studentory의 기존 파스텔 톤, 라운드 패널, 디자인 토큰과 접근성 기준을 유지한다.

## 2. 목표

수업 현황의 기본 날짜 보기를 다음 형태로 변경한다.

```text
                 월요일       화요일       수요일       ...       일요일
13:00            수업 카드     빈 칸         빈 칸                  빈 칸
13:10            빈 칸         수업 카드     빈 칸                  빈 칸
13:20            빈 칸         빈 칸         수업 카드              빈 칸
 ...
23:00            빈 칸         빈 칸         빈 칸                  수업 카드
```

핵심 목표는 다음과 같다.

1. 한 주의 수업 분포를 요일과 시간 기준으로 즉시 파악한다.
2. 수업이 없는 시간도 빈 슬롯으로 표시해 비어 있는 시간을 확인한다.
3. 수업 카드에서 학생의 성별, 이름, 단계, 나이를 빠르게 확인한다.
4. 상태 변경은 수업 상세 모달에서만 수행해 실수 가능성을 줄인다.
5. 완료된 수업을 다시 미완료 상태로 되돌릴 수 있게 한다.
6. Desktop, Compact, Mobile에서 데이터 누락이나 조작 불가능 상태가 없어야 한다.

## 3. 구현 전 필수 절차

Codex는 바로 수정하지 말고 다음 순서로 진행한다.

1. 루트 `AGENTS.md`, `frontend/AGENTS.md`, `backend/AGENTS.md`를 읽는다.
2. 현재 브랜치와 `git status`, 전체 미커밋 diff를 확인한다.
3. `LessonPage`, `LessonRecordModal`, lesson API client/type, FastAPI lesson router/service/test를 분석한다.
4. 기존 `UIR-001` 디자인 토큰과 공통 `Modal` 동작을 확인한다.
5. API와 상태 전이 변경 범위를 먼저 정리한 뒤 구현한다.
6. 현재 작업 트리의 기존 변경을 되돌리거나 덮어쓰지 않는다.
7. 사용자가 별도로 요청하지 않는 한 커밋, 푸시, PR 생성은 하지 않는다.

서버는 사용자가 직접 구동한다. Codex는 기존 서버를 종료하거나 재시작하지 않는다. 백엔드 또는 프론트엔드 재시작이 검증에 필요하면 필요한 이유와 명령을 사용자에게 알리고 기다린다.

## 4. 주간 시간표 레이아웃

### 4.1 축과 범위

- 가로축은 월요일부터 일요일까지 7개 요일이다.
- 세로축은 `13:00`부터 `23:00`까지다.
- 10분 단위의 시작 시각을 모두 표시한다.
- `13:00`, `13:10`, ..., `22:50`, `23:00`의 총 61개 시간 행을 만든다.
- API에 수업이 없어도 모든 시간 행과 요일 셀을 렌더링한다.
- 시간표 데이터는 현재 선택한 주의 `lesson_date`와 `lesson_time`을 기준으로 배치한다.
- 현재 데이터 모델에 수업 종료 시각이나 수업 시간이 없으므로, 한 수업은 해당 시작 시각의 한 셀에 배치한다. 임의로 수업 길이를 추정하거나 여러 행을 병합하지 않는다.

### 4.2 시간 정규화

- 수업 시작 시각이 정확한 10분 단위이면 일치하는 슬롯에 배치한다.
- 기존 또는 수동 데이터가 10분 단위가 아니면 가장 가까운 슬롯으로 임의 반올림하지 않는다.
- 10분 단위가 아닌 수업은 원래 시각을 보존하고 해당 10분 구간의 셀에 배치한다. 예: `17:07`은 `17:00` 행에 표시하되 카드에는 `17:07`을 표시한다.
- 같은 날짜와 같은 시간 구간에 여러 수업이 있으면 한 셀 안에서 카드들을 세로로 쌓는다. 어떤 수업도 덮거나 숨기지 않는다.
- `13:00` 이전 또는 마지막 슬롯 범위가 끝나는 `23:10` 이후의 수업은 버리지 않는다. 시간표 아래에 `표시 범위 밖 수업` 영역을 두고 원래 날짜와 시각으로 접근 가능하게 표시한다.

### 4.3 스크롤과 고정 영역

- 시간표 본문은 세로로 스크롤할 수 있어야 한다.
- 요일 헤더는 시간표 내부 세로 스크롤 중 고정한다.
- 시간 열은 가로 스크롤이 필요한 레이아웃에서 고정한다.
- 페이지 전체가 61개 행 때문에 무제한으로 길어지지 않도록 시간표 컨테이너에 뷰포트에 맞는 최대 높이를 둔다.
- 휠, 트랙패드, 키보드만으로 모든 요일과 시간 슬롯에 접근할 수 있어야 한다.
- 고정 헤더와 카드가 겹치거나 포커스 링을 가리지 않아야 한다.

## 5. 수업 카드 정보 구조

각 수업 카드는 다음 순서로 표시한다.

```text
[성별 표시] 학생 이름 [단계 배지] [약 N세 배지]
실제 시작 시각
[수업 상태] [준비 상태] [출결 상태]
```

### 5.1 학생 식별 정보

- 이름 왼쪽에 성별을 표시한다.
- 성별은 `MALE → 남`, `FEMALE → 여`로 사용자에게 전달한다.
- 성별을 색상이나 이모지만으로 구분하지 않는다. 보이는 짧은 라벨 또는 스크린 리더용 텍스트를 함께 제공한다.
- 이름 오른쪽에 학생의 `stage` 배지와 나이 배지를 배치한다.
- 단계는 기존 사용자 용어인 `1단계`부터 `6단계` 형식을 사용한다.
- 나이는 전체 생년월일이 아니라 `birth_year`만 있으므로 `현재 연도 - birth_year`로 계산한 근사값이다.
- 나이 배지는 오해를 막기 위해 `약 N세`로 표시한다.
- 레거시 데이터에서 생년 또는 성별이 없을 가능성을 안전하게 처리하고 각각 `나이 미입력`, `성별 미입력`로 표시한다.

### 5.2 상태 정보

- 카드에는 수업 상태, 준비 상태, 출결 상태를 읽기 전용 배지나 짧은 텍스트로 표시한다.
- `SCHEDULED`, `COMPLETED`, `CANCELED`는 색상 외에 각각 `수업 예정`, `수업 완료`, `수업 취소` 문구로 구분한다.
- 출결은 `출석`, `결석`, `출결 미입력`으로 표시한다.
- 준비는 `준비 완료`, `준비 필요`로 표시한다.
- 취소된 수업도 원래 시간 슬롯에 남기고 취소 상태를 명확히 표현한다. 숨기거나 빈 슬롯으로 바꾸지 않는다.

### 5.3 카드 상호작용

- 카드 전체를 하나의 명확한 수업 상세 진입점으로 제공한다.
- 마우스 클릭과 키보드 Enter/Space로 `LessonRecordModal`을 연다.
- 카드 안에 출석, 결석, 완료, 취소, 복구 같은 즉시 변경 버튼을 두지 않는다.
- 시간표에서 직접 API mutation을 발생시키지 않는다.
- hover와 `:focus-visible` 상태를 제공한다.
- 접근 가능한 이름에는 학생명, 요일 또는 날짜, 실제 수업 시각을 포함한다.

## 6. 모달 중심 상태 변경

수업 취소/복구, 완료/미완료 복구, 출석/결석 변경은 모두 `LessonRecordModal` 안에서만 수행한다.

### 6.1 폼 상태

- 준비 상태, 출결, 단계, 특이사항, 수업태도는 모달 폼에서 편집한다.
- 출결은 `미입력`, `출석`, `결석`을 선택할 수 있어야 한다.
- `저장`은 폼 데이터만 저장하며 수업 lifecycle을 암묵적으로 변경하지 않는다.
- `변경 취소`, 닫기, Escape는 저장하지 않은 폼 변경을 폐기한다.
- lifecycle action 전에 저장이 필요한 경우 기존 `completeSelected` 흐름처럼 순서를 코드에서 명시하고, 부분 실패 시 사용자가 무엇이 저장되었는지 알 수 있게 오류를 표시한다.

### 6.2 상태별 lifecycle action

| 현재 상태 | 모달에서 제공할 lifecycle action | 결과 상태 |
| --- | --- | --- |
| `SCHEDULED` | `수업 취소` | `CANCELED` |
| `SCHEDULED` | `수업 완료 처리` | `COMPLETED` |
| `CANCELED` | `수업 복구` | `SCHEDULED` |
| `COMPLETED` | `미완료로 되돌리기` | `SCHEDULED` |

- 완료 처리에는 현재 확정된 규칙대로 출결 입력이 필요하다.
- 취소와 미완료 복구는 의미가 다르므로 같은 API나 버튼 문구를 재사용하지 않는다.
- 수동 수업 삭제는 기존 규칙을 유지하고 lifecycle action 그룹의 가장 왼쪽에 둔다.
- 파괴적이거나 기록 의미가 바뀌는 action에는 확인 문구를 제공한다.
- lifecycle action은 왼쪽, `변경 취소`와 `저장`은 오른쪽이라는 기존 모달 계층을 유지한다.
- action 완료 후 목록의 해당 카드가 새 상태로 즉시 갱신되어야 한다.
- 중복 요청을 막기 위해 처리 중 관련 버튼을 비활성화하고 상태를 사용자에게 알린다.

## 7. 완료 취소 API 계약

기존 `POST /lessons/{lesson_id}/restore`는 `CANCELED → SCHEDULED` 전용으로 유지한다.

완료된 수업을 다시 미완료로 돌리는 별도 명령을 추가한다.

```http
POST /lessons/{lesson_id}/reopen
```

### 7.1 성공 계약

- 허용 전이: `COMPLETED → SCHEDULED`
- 응답: `200 OK`
- 응답 본문: 갱신된 `LessonResponse`
- 같은 lesson ID, 날짜, 시간, 학생, 일정 연결을 유지한다.
- 출결, 준비, 단계, 특이사항, 수업태도는 자동 삭제하지 않고 보존한다.
- DB 컬럼이나 새 persistent 상태를 추가하지 않는다.

### 7.2 오류 계약

- 존재하지 않는 수업은 기존 오류 매핑과 동일하게 `404`를 반환한다.
- `COMPLETED`가 아닌 수업에 호출하면 `422`를 반환한다.
- 오류 메시지는 `Only completed lessons can be reopened`처럼 허용 상태를 분명히 설명한다.
- 동일 명령의 반복 호출은 두 번째 호출에서 `422`가 되어야 한다.

### 7.3 예상 수정 지점

- `backend/src/backend/app/api/v1/endpoint/lesson.py`
- `backend/src/backend/app/service/lesson_service.py`
- `backend/tests/test_lesson_api.py`
- `frontend/src/api/lessons.ts`
- 필요 시 frontend lesson type과 E2E mock

새로운 상태 enum이나 DB migration은 필요하지 않다. 서비스 계층이 전이 규칙을 소유하고 router는 얇게 유지한다.

## 8. 프론트엔드 구현 구조

현재 `LessonPage.tsx`에 모든 시간표 계산과 JSX를 더 쌓지 않는다. 다음처럼 책임을 나누는 방식을 우선 검토한다.

```text
LessonPage
├── 주간 조회·API orchestration
├── WeeklyLessonTimetable
│   ├── 시간 슬롯 생성
│   ├── 날짜/시간별 수업 그룹화
│   └── LessonTimetableCard
└── LessonRecordModal
    ├── 기록 폼
    └── 상태별 lifecycle action
```

- 실제 복잡도가 작으면 불필요한 추상화를 만들지 않는다.
- 시간 슬롯 생성과 수업 배치 계산은 순수 함수로 분리해 단위 테스트 가능하게 한다.
- 날짜와 요일 계산은 기존 주간 기준 함수를 재사용한다.
- 학생 lookup은 현재 `studentsById` 흐름을 재사용한다.
- `학생별 보기`가 유지된다면 동작을 깨뜨리지 않는다. 이번 시간표 변경은 기본 `날짜 보기`에 국한한다.
- 기존 디자인 토큰과 공통 `Modal`을 재사용하고 새 스타일 라이브러리를 추가하지 않는다.

## 9. 반응형 요구사항

### Desktop — 1440 × 900

- 시간 열과 월요일~일요일 7개 열을 한 화면에서 우선 표시한다.
- 카드의 이름, 단계, 나이 정보가 불필요하게 여러 줄로 쪼개지지 않아야 한다.
- 셀이 좁을 때 이름을 무조건 잘라내지 말고 배지 배치와 카드 내부 줄바꿈 우선순위를 조정한다.

### Compact — 1024 × 768

- 7개 요일을 지나치게 압축해 읽을 수 없게 만들지 않는다.
- 시간 열은 고정하고 시간표 영역에 제한된 가로 스크롤을 허용할 수 있다.
- 페이지 전체에는 가로 overflow가 생기지 않아야 한다.
- 현재 요일 또는 사용자가 보고 있던 열을 쉽게 유지할 수 있어야 한다.

### Mobile — 390 × 844

- 7열을 390px에 억지로 축소하지 않는다.
- 요일 선택 탭 또는 하루 단위 보기로 전환해 선택한 요일의 시간 행을 표시한다.
- 월요일~일요일 모두 키보드와 터치로 선택 가능해야 한다.
- 시간 행과 빈 슬롯을 유지하며 카드 정보와 모달 진입 기능을 제거하지 않는다.
- 페이지 전체 가로 스크롤은 없어야 한다.
- 모달은 내부 세로 스크롤로 모든 필드와 footer action에 접근할 수 있어야 한다.

## 10. 접근성 기준

- 시간표는 시각적 grid이지만 무조건 ARIA `grid`를 도입하지 않는다. 완전한 grid 키보드 모델을 구현할 수 없으면 의미 있는 heading, list, button 구조를 사용한다.
- 요일과 시간의 관계를 스크린 리더가 이해할 수 있도록 카드의 accessible name 또는 설명에 날짜와 시각을 포함한다.
- 빈 셀은 키보드 tab stop으로 만들지 않는다.
- 수업 카드와 모달 action은 자연스러운 탭 순서를 가져야 한다.
- 모달 최초 포커스, Escape 닫기, focus trap, 닫은 뒤 카드로 포커스 복원을 유지한다.
- 상태를 색상만으로 전달하지 않는다.
- 긴 이름과 브라우저 확대 200%에서도 핵심 조작이 가능해야 한다.

## 11. 로딩·빈 상태·오류

- 로딩 중 시간표 골격과 데이터 없음 상태를 구분한다.
- 해당 주에 수업이 하나도 없어도 시간축과 빈 슬롯을 보여준다. 별도 안내 문구로 `선택한 주의 수업이 없습니다.`를 함께 제공할 수 있다.
- 학생 lookup이 실패하거나 없는 student ID를 가진 lesson이 있어도 화면 전체를 중단하지 않는다.
- 주간 API 실패 시 기존 오류 패턴과 재시도 경로를 유지한다.
- lifecycle 또는 저장 API 실패 시 모달을 닫지 않고 오류와 재시도 가능 상태를 유지한다.
- 범위 밖 수업이 있으면 별도 영역으로 알려 데이터가 사라진 것처럼 보이지 않게 한다.

## 12. 테스트 요구사항

### Backend

`backend/tests/test_lesson_api.py`에 최소 다음 계약을 추가한다.

- 출결이 있는 예정 수업을 완료할 수 있다.
- 완료 수업을 `POST /reopen`으로 예정 상태로 되돌릴 수 있다.
- reopen 후 ID와 기록 필드가 보존된다.
- 예정 또는 취소 수업에 reopen을 호출하면 `422`다.
- 존재하지 않는 수업은 `404`다.
- reopen 반복 호출의 두 번째 요청은 `422`다.
- 기존 취소 복구 API는 계속 `CANCELED → SCHEDULED`만 허용한다.

### Frontend / Playwright

최소 다음 동작을 자동화한다.

- 13:00~23:00의 첫 슬롯과 마지막 슬롯이 렌더링된다.
- 수업이 없는 중간 시간에도 빈 행이 유지된다.
- 월요일~일요일 헤더가 올바른 날짜와 연결된다.
- 정확한 10분 수업과 10분 단위가 아닌 수업이 사라지지 않는다.
- 같은 시간 구간의 복수 수업이 모두 표시된다.
- 카드에 성별, 이름, 단계, 근사 나이가 표시된다.
- 카드 클릭, Enter, Space가 모달을 연다.
- 카드에는 출석/결석, 완료/취소/복구 mutation 버튼이 없다.
- 출결은 모달 저장을 통해서만 변경된다.
- 예정→완료→미완료 복구가 같은 레코드에서 동작한다.
- 예정→취소→복구가 같은 레코드에서 동작한다.
- 미저장 변경이 `변경 취소`, 닫기, Escape로 폐기된다.
- API 실패 시 모달이 유지되고 오류가 표시된다.
- Mobile 요일 전환과 빈 슬롯 접근이 가능하다.
- Desktop, Compact, Mobile에서 페이지 전체 가로 overflow가 없다.

테스트 mock은 API host가 `localhost` 또는 `127.0.0.1`인 경우를 모두 지원하되, 프론트엔드 문서 URL을 가로채지 않도록 반드시 API 포트 `8000`까지 제한한다.

## 13. 직접 UI 검증

서버 재시작이 필요하면 사용자에게 요청한 뒤 다음 환경에서 실제 브라우저로 확인한다.

| Viewport | 필수 확인 |
| --- | --- |
| `1440 × 900` | 7개 요일, sticky header/time rail, 카드 밀도, 13:00과 23:00 접근 |
| `1024 × 768` | 시간표 내부 가로 스크롤, 시간 열 고정, 페이지 overflow 없음 |
| `390 × 844` | 요일 선택, 빈 슬롯, 카드 정보, 모달 footer 접근, 가로 overflow 없음 |

추가로 다음 상태를 확인한다.

- 수업이 없는 주
- 한 셀에 수업이 여러 개인 경우
- 긴 학생 이름과 긴 단계 기록
- 예정, 완료, 취소 수업
- 출결 미입력, 출석, 결석
- API 오류
- 키보드 focus와 모달 복원
- 브라우저 콘솔 오류

UI 검증은 `review-ui` Skill을 따르고 결과를 `docs/LOOP_LOG.md`에 기록한다.

## 14. 검증 명령과 완료 gate

구현 후 `verify-cross-stack` Skill과 저장소의 표준 명령을 사용한다.

```powershell
./scripts/verify.ps1 -Scope all
```

Playwright를 별도로 실행해야 하면 frontend에서 실행한다.

```powershell
pnpm e2e
```

로컬 검증 후에도 GitHub Actions의 lint → type-check → test → build → Playwright와 CodeQL이 최종 gate다. 사용자가 커밋과 PR을 요청하지 않았다면 로컬 결과까지만 보고하고 CI를 통과했다고 표현하지 않는다.

## 15. 범위 제한

- 학생, 수업, 기존 상태 데이터를 삭제하거나 초기화하지 않는다.
- 시간표 구현을 위해 DB 스키마나 lesson status enum을 늘리지 않는다.
- 수업 시간을 임의로 추정하지 않는다.
- 준비 상태, 출결, 완료 상태를 카드 클릭만으로 바꾸지 않는다.
- 기존 학생별 보기, 수업 생성, 기록 저장, 취소 복구를 깨뜨리지 않는다.
- 다른 페이지의 레이아웃을 변경하지 않는다.
- 브랜드 이미지, SVG, 새로운 문구성 장식을 추가하지 않는다.
- 새 UI 또는 날짜 라이브러리는 기존 기술로 해결할 수 없다는 근거가 없으면 추가하지 않는다.
- `backend/src/backend/app/core/database.py`와 `backend/init.sql`은 이 작업 범위 밖이다.
- 실행 중인 사용자 서버를 Codex가 임의로 종료하거나 재시작하지 않는다.

## 16. 완료 조건

다음 조건을 모두 충족해야 구현 완료로 보고한다.

- [ ] 현재 미커밋 `UIR-001` 변경이 보존되었다.
- [ ] 월요일~일요일이 가로축으로 제공된다.
- [ ] 13:00~23:00의 10분 단위 빈 시간 행이 모두 존재한다.
- [ ] 수업이 정확한 날짜와 시간 구간에 표시된다.
- [ ] 범위 밖 또는 비정규 시각 수업이 유실되지 않는다.
- [ ] 성별, 이름, 단계, 근사 나이가 카드에서 읽힌다.
- [ ] 수업 상태와 준비·출결 상태가 색상 외 문구로 식별된다.
- [ ] lifecycle과 출결 mutation이 모달에만 존재한다.
- [ ] 완료 수업의 미완료 복구 API와 UI가 동작한다.
- [ ] 취소 복구와 완료 복구가 서로 다른 명령으로 유지된다.
- [ ] Desktop, Compact, Mobile 요구사항을 충족한다.
- [ ] 접근성, 오류, 긴 데이터, 복수 수업 상태를 검증했다.
- [ ] 백엔드·프론트엔드·Playwright 검증이 통과했다.
- [ ] 전체 diff를 검토했고 범위 밖 변경이 없다.
- [ ] `docs/LOOP_LOG.md`에 실제 검증 근거가 기록되었다.
- [ ] 커밋·푸시·PR·CI 상태를 사실대로 구분해 보고했다.

## 17. 최종 보고 형식

구현을 마친 Codex는 다음을 간결하게 보고한다.

1. 변경한 시간표 구조
2. 카드 정보와 상호작용
3. 모달 lifecycle 변경
4. 추가한 `reopen` API 계약
5. Desktop/Compact/Mobile 검증 결과
6. backend/frontend/Playwright 검사 결과
7. 기존 작업 보존 여부와 전체 diff 범위
8. 남은 위험 또는 미검증 항목
9. 서버 재시작 또는 사용자 시각 검토가 필요한지 여부
